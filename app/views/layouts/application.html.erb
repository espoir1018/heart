<!DOCTYPE html>
<html lang="en">
<head>
  <title>Heart</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>

<body data-controller="app" class="bg-body-secondary">
<%= render partial: 'layouts/default/menu' %>
<div class="app-main flex-grow-1 d-flex flex-column flex-nowrap">
  <%= render partial: 'layouts/default/navbar' %>
  <main class="app-content flex-grow-1" data-app-target="main">
    <%= render partial: 'layouts/default/breadcrumb' %>
    <%= yield %>
  </main>
  <%= render partial: 'layouts/default/footer' %>
</div>
</body>
</html>
