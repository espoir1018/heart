<!DOCTYPE html>
<html lang="en">
<head>
  <title>Heart</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>

<body data-controller="app" class="bg-body-secondary">
<%= render(AsideComponent.new) %>
<div class="app-main flex-grow-1 d-flex flex-column flex-nowrap">
  <%= render(HeaderComponent.new(user: current_user)) %>
  <main class="app-content flex-grow-1" data-app-target="main"><%= yield %></main>
  <%= render(FooterComponent.new(user: current_user)) %>
</div>
<%= toastr_flash_tag %>
</body>
</html>
